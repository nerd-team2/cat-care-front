import "./globals.css";


export const metadata = {
  title: "Cat Care",
  description: "cat care website",
};

export default function RootLayout({ children }) {
  return (
    <html>
      <body>
        <h1>ass</h1>
        {children}</body>
    </html>
  );
}
